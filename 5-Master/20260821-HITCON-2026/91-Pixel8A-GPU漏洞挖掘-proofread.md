---
title: "Google Pixel 8A Mali GPU Driver 漏洞挖掘與提權實戰 (PK)"
type: "verbatim-narrative-transcript"
verbatim: true
---

# 🎙️ Google Pixel 8A Mali GPU Driver 漏洞挖掘與提權實戰 (PK)

> **【排版與校對說明】**：本文件為 **100% 全篇原話逐字稿深度校對與排版版**。保留講者所有原話發言、語意轉折、現場互動對話、幕後故事與問答，**未做任何刪減或摘要縮寫**；已全面修訂語音辨識錯字、同音字與專有名詞，並依演講敘事邏輯完成流暢的段落劃分與主題標題標註。

---

我們切換好，大家好，我是 PK， 然後我看那個議程 R2 都是跟那個 PC 有關，那至少大家說就是接下來講沒有沒有 PC 的東西。那我接下來要這場主要要分享的是跟 Android 相關的研究，然後主要是 Mali GPU 在這上面的一些漏洞挖掘跟利用

其實我在去年（2024 年）的時候在 Android 核心上面第一次踏入這個研究領域，運氣還不錯，就是找到一些漏洞，然後最後也有成功寫出完整的 Exploit 在實機手機上面拿到 Full Root，對，然後我覺得整個利用手法的探索或者挖掘的過程都還蠻有趣的，所以想在現場跟大家分享一下。然後我目前在單位是擔任研究員，然後我平常比較 Focus 的領域是 System，跟可能 Browser 也有一點點小的研究這樣子

那今天的大綱大概是長這樣，就是首先我會先介紹一下硬體與驅動的一些簡介，然後 Focus 在今天的主題是 Mali GPU driver，ARM 推出的一個 GPU 它的 driver。那接下來會講兩個漏洞，那兩個漏洞的成因講完之後會在後面另外其中一個漏洞是 CVE-2025-6349 的完整利用，然後就有一個 Demo 的影片，然後講一下一些新的心得分享

好，那我的


## 🎯 研究目標：Google Pixel 8A 與攻擊情境設定

因為我之前是做一般系統研究，那我覺得好像可以試試看一些比較困難的東西，那又不想要處理變得太多，所以我就稍微研究一下發現手機好像是一個不錯的主題，那我的目標很簡單，就是我就是身為一個攻擊者，目標受害者的手機上面裝了我自己寫的一個 App。那這個 APP 呢，它前端可能是跑一些 UI 啊，正常的就是模擬一個正常的 App，但背景跑一個 Exploit，那最後在 Exploit 結束之後呢，會回傳一個 Full Root Shell 給攻擊者，那這個就是我預期中我想做到事情

那我選了這個 Google, 因為 Google 手機有很多，那 Samsung 或是 Google 的 Pixel，那我最後是選了 Pixel 8A 這個型號。那第一個第一個是因為當時是我第一次研究，所以我希望可以選一個比較便宜一點的手機，就算就算研究失敗了還是不會虧錢

對，那再來是當時的那個 8A 呢，還是處於一個比較沒有到很舊，也沒到很新的一個機，所以好像也可以接受。那最後它是最蠻重要的是就是在當時做研究的時候，網絡上有很多跟 Google 跟 Pixel 相關的研究，那 8 的話又就是八系列的話又還蠻多的

那因為做資安研究肯定免不了去參考前人研究，所以覺得這樣有作業可以抄，選這個機子就對了。那你可能就是沒有做過 Android 研究的話，就是好奇 Android 的一些。選不選差了差了多少，那簡單來講是他們一個一個是指令集不一樣，通常我們加一些電腦都是跑在 x86_64, 那 Android 因為是手機的關係，所以都使用 ARM64 比較多

那另外他們都有在對於 Kernel 來說呢，它們都有內建的一些 Mitigation，像是 KASLR 或是 Stack Protector, 那對應到上面也有相同的保護機制。那除此之外呢，手機會額外開一些更先進的保護措施，那不管是軟體層級或是硬體層級，像是 PAN、PAC、CFI 或是 MTE，那 iOS 可能又更多一點

那最後的話SELinux 的話可以說是 Android 的大掌櫃。那為什麼呢？是因為這個保護機制它可以限制，它可以限制一些，它可以有效限制來路不明的 App，規範了每個 App 可以做什麼事。對於來路不明的 App 執行時，SELinux 給的權限非常非常少

所以你有些功能甚至連碰都碰不到，那碰不到的話就不要扯說就是可能更提到什麼挖掘漏洞之類的，因為根本都沒辦法去使用那個功能。所以攻擊裡面就會相少很多。那對於一個 app 來講有什麼樣的攻擊面呢？第一個部分是因為它因為畢竟還是非所以一些 Core 的核心子系統，像是常見的免不了的什麼 Network 或是 Memory Management, 這些都還是算在一個攻擊面裡面

那再來是因為 Android 架構的關係，它需要頻繁做 Process 之間的 IPC 互動，也是用 driver 處理，這邊有兩個重要 driver 是 Binder 跟 ashmem 共享記憶體 那一個一個是用來去關管理資料的怎麼傳輸，一個事情來去管理他們共享的資料是怎麼怎麼做使用的。那最後一個部分，因為手機上面通常會有很多額外的硬體，像是 GPU、NPU 等，要使用這些硬體就必須要有核心 driver

手機晶片商通常跟第三方公司購買硬體，把第三方的硬體 GPU driver 也加到他們的 Source Code，應該說開最後出的那個產品裡面。那因為有些 Untrusted App 可能需要圖形渲染功能，所以在 SELinux 的保護機制底下，Untrusted App 就可以被允許使用 GPU 或硬體功能是硬體的一些一些功能，對

那通常因為這個 driver 的程式碼都是由第三方去提供的，那手機廠商並沒有辦法很好地保證第三方驅動品質，所以在這部分也是大家最常尋找的攻擊面。那在做研究之前呢，當然要去整理一下過去的研究，那我參考了一些像 project Zero 或是一些公開的 Repository 或是一些 Conference 資源，整理過去 Android 核心漏洞挖掘與利用的統計

那對最後就這張圖表大概就是雖然可能沒有到很精確，但大概就是整理的一些資源所畫出來的表。那可以看到


## 🔬 建立除錯環境：CONFIG_MALI_BIFROST_NO_MALI 與 kprobes

基本上絕大多數漏洞都是第三方的 driver, 然後這個好像也還蠻直觀的。但如果我們再劃分的更細一點去看的話，我們把它分成是 GPU 跟 GPU。你會發現說 GPU 竟然佔了一半以上的一半以上的漏洞的來源。所以代表如果你要找漏洞的話，那就是去找 GPU 的漏洞， GPU 上面的漏洞，那這個非常合理，所以我就是開始做研究，然後研究目標就是 Google Pixel 8A 上的 Mali driver，目標是找一個提權漏洞拿到 Full Root

那手機買了，但是要你可能要去做 debug 還是有點麻煩，因為手機不像一般 Linux Distribution 可以隨便用 QEMU 開機跑起來，因為有很多硬體 Dependencies，模擬相對困難，所以但是因為我選的目標是 Mali GPU, 那 Mali GPU 有一個它的 driver 有一個非常對研究友善的一個 config option 叫做 MALI_BIFROST_NO_MALI。那為什麼它是研究有善呢

是因為你可以你可以在沒有一題的情況下把這個給編譯起來，然後你就可以在你的 local 的 VM 給它跑起來。然後跑起來的話，你可能會想說那硬體的部分怎麼辦？那它這個 driver 在你開啟這個選項之後呢，它 Backend 會去處理硬體模擬，雖然只是做一些 Dummy 操作，但 95% 的 driver 功能都還是可以正常運作，那這樣我們就可以在我們的 local 呢做研究

非常的簡單。那再來你找漏洞之後呢，你可能會需要在還是要在實機上面做測試，因為 Local 環境跟手機環境肯定是不一樣的。那這時候稍微做了一下研究，會發現比較直觀的做法是直接從編譯一個 roid 的 c, 然後把它燒回去手機上面。那然後，然後從 B 的時候你就啟用了 CELB 的這個選項

那這個選項就是如果成功用之後，它會像是你在邊的 s 那樣的滑順。但是它要啟動有一些有些條件，就是可能會需要在 D 的時候要有額外的硬體。那另外是因為我是自己做自己是第一次做這個研究，所以我可能會不把它弄壞，那我的手機就廢掉了，然後我研究沒救成，我就噴了一堆錢，那我自己不想要這樣子

所以我就開始找一下其他的解決方法。然後後面找到兩個方法是下就是它沒有 GP 這麼的厲害，但是它還是用起來非常的非常的方便，那第一個是 kprobes, 第二個是 procfs, 那 kprobe 是這兩個是在你只要有的情況下，你就可以去做使用的一個 Linux 提供， Linux 提供的一個指系統的功能，那就是你可以去一些 function, 然後在這個 function 被執行到的時候呢，印出一些 argument 資訊

那或者甚至你可以去 d 這些或是把些分拿出來做分析。那另外一個是 pf 你可以去寫一個 program, 那這個 program 它就可以，你就可以給它指定一個虛擬記憶體位置，然後它就把這個記體位置裡面的資料呢可以進出來。那實際使用情況會長像是這樣，就是首先你會用個 KP 的方式呢，去抓去一些你想要去可能你的有問題的可能 UAF 的 F 啊，他們會傳到的這個 function, 那你去 hook 它，然把 argument 給或是 return 給印出來

那第二個印出來之後，你在做 him spray 之後呢，你可能會你可能會想知道是不是 Heap Spray 有成功，然後你就會把這個 address 給 procfs, 那它就會把資料給出來，那這樣的話在 B 的時候就會非常非常的方便，那你也不用重新做 build 了。好，那這次講完這個 debug 的環境之後呢，再回到我們的主題是 M 然後接下來我會講一下跟


## ⚙️ Mali GPU 驅動架構：kbase 與 Queue 機制

Mali GPU 驅動一些比較重要的架構背景知識，然後不會 cover 到所有的 M 的一些實作。那首先我們先講一下架構，那用 MQ 非常非常簡單，是它是個 Character Device，開啟後會建立 file object，接著有 kbase_file 與 kbase_context

kbase_context 是核心最關鍵的結構，因為它就是一個很大很大的結構，然後管理的裡面基本上操作的所有的資源都是由它去做 reference 的。然後裡面有些結構，但比較重要比較 genalgen 成面比較重要的是 kbase_va_region，是用來管理 GPU 與 CPU Memory Mapping 的物件

那除了這個 region 之後之外呢，這下會講一下跟兩個跟兩個跟漏洞有關的機制。那第一個是叫 kcpu_queue, 那它是一個用完去把 CPU 端的操作呢丟給 backing worker 的一個一個功能，那使用方式非常簡單，就是你一個 cube, 然後就 ksc

然後你建完之後呢，你可以 queue 進去就是做些操作，像是這邊的操作基本上你可以想成是你就把 iOS 的 command 就給後端的 w 去做這樣這樣這樣的這樣的概念。所以你 inqute 完之後呢，你去叫下 worker 執行，那 worker 呢就會再跑在 worker 去處理這些個請求

對，然後這就是 kcpu_queue 的功能。那另外功能是 kbase_csf_queue (CSQ)，相對於前面 CPU 的操作，這邊屬於 GPU 操作，只是他們的原理都非常非常的相似。那你在用這個 CSQ 的時候，你會要建 C 嘛，然後你要先有一個 region, 然後這邊假設這個 region 是差 1000 到差 2000, 然後就可以用借出來

那這個 region 呢，它會在我們的目標的 CSQ, 就是 KQ, 在這邊是 KQ, 然後起來，然後你在 ace 就可以做 memory mapping, 然後把你的一些 GP 的給塞進去。然後最後一樣是 region 這個 queue, 然後 queue worker 呢就會把這些 command 呢給 queue 出來，然後轉是 GPU 可以看的形式，然後叫 GPU 執行

好，那差不多就是這兩個機制是跟漏洞有關的，那接下來會講一下這兩個這兩個機制底下各出了一個的各出了一個漏洞。他們的漏洞前。那第一個漏洞是


## 💥 漏洞一：CVE-2025-8045 Double Free 成因

CVE-2025-8045。這個漏洞出在 Dump Debug 功能中。kbase_kcpu_queue 可以 Enqueue CPU 命令，過程中可以註冊一個 Dump Buffer，你可以先隨便塞任何，像這邊是那完之後呢，你的這個東西會在 Worker 在處理命令過程中發生錯誤（如 Timeout）時，會把 Dump Buffer 拿來印出錯誤資訊

那使用方式也很簡單，就是你在因為你的 wor 發生問題，就是處理 command 的過程中發生問題，那它需要一些錯誤資訊在 c 上面，那當 error 有點像是對給開發的一個一個機制是讓你知道說目前的 DER, 你目前的 console 是在 C 是在哪地方。那我們舉個實際的例子會比較會比較好理解

那像這張圖是在沒有任何當的時候給給在發生 W 發生問題的時候印出的這些 console 你會發現說這邊簡單的印行是當 arrow, 然後什麼都沒有發什麼都沒有出現。那如果我們塞了，我們先註冊了一個 buffer, 然後塞了一大 A, 然後在 W 的時候呢，它就會把這個 A 呢一起出來

那想像好，雖然可能跟 debug 有關係，但直觀上好像還是沒辦法想像它的功能到底是什麼。不過既然它都是一個功能，所以當然要看一下它有沒有出現問題。那我在看它的實作的時候，我當我看到這個，因為它程式碼其實沒有很多，但是我看到這個程式碼的時候就覺得好像這個這個寫法好像哪邊有問題

那對基本上我看到的時候我就發現說這個 呼叫 Free 前面沒有任何檢查直接釋放，那你如果實際上追的話，你發現說好像好像就剛剛好的避免掉一些問題，就是它每次呼叫釋放函式的時候，你的都會是不同的 F。但你這時候就會說是不是下面的 status 啊有沒有一些在一些情況下面 condition

這種這種這種問題，然後導致說這個 buffer 它不會一直不一樣的，它可能是一直是同一個 buffer, 那如果是的話，它就會出現可以做的 double free 的問題。那確實在這個假設情況下，我們找到了一個漏洞。那漏洞情境是你有一個 thread one 跟 thread 2, 那 one 是負責去送 command

然後送給 walker, 然後這這時候 walker 呢，他在處理的時候它就會出現問題，然後我們我們讓它執行的過程中發生 timeout 的事件，那代表是 arrow, 所以 W 會去處理這個 arrow, 那 worker 做的第一件事情不是去直接印 carrow, 它是會先去等 error 註冊，那有的話呢，它就會去把這個資料給印出來，那之後我們叫另外一個 thread 去呼叫這個，然後去註冊一張 BF, 那這時候剛個奇怪的 C 就被執行到但應是我們第一次執行，所以 kfree 的這個 Fer 呢會升到不會有任何的問題。但是我們讓這個 R 讓這個我們讓這個註冊等待註冊的這個操作跟註冊這個操作呢出現 race condition, 所以這時候我的 S 呢他等到一半發現總都沒有當然就不想等，所以發生開

那另外一邊其實它已經正在註冊了啊，那這時候他在更 status, 那左邊右邊的更新 status。然後左邊發現發生準備離開，這時候這個 race condition 會造成是我這個 buffer 會被註上去，但是我的狀態已經已經開始已經出現錯誤了

所以下次我在註冊我在重新註冊這個 dump buffer 的時候呢，我的這個 buffer 呢會一直會是上一會是上一個 register 的 b address, 但然後一樣是 C 嘛，但是因為狀態已經壞掉了，所以我可以還有 return, 那我就可以直接就可以一直呼叫這個去附加這個 buffer 非常非常非常弱次。所以這是一個 ra transition 所造成的這個 dble, 然後這個 dble fre 呢它的 b 可以扣，那它也可以去觸發非常非常多

所以它是相比較簡單做利用的。那下一個漏洞是 CVE-2025-6349, 那這個漏洞它變這個漏洞的就是有點有點賽到，帶有一點運氣成分，那它被一個還蠻有趣的小故事。首先因為在做研究的時候，你不了去看一下前人的智慧，就看一下說過去啊都這個 GP 都發生怎樣類型的漏洞

所以你在做 L 研究的時候呢，你可能會想先從開始分析。那就看到 Project Zero 發了一篇當時很新的部落格，我就想辦法照著敘述去複現 1-Day 漏洞。那在執行的過程中，在複的過程中呢，就發我就發現了一個奇怪的奇怪的操作，就是它 m 他去呼叫 mmap 嘗試把 Queue Handle 映射出來，參數是 0x3000

然後當下看可能就會覺得這個零差 3000 是什麼意思，然後再還沒有了解，在還沒有知道整個架落的情況下，看到這三天會考慮。所以我那時候就我隨意改了一下 Size 參數，結果系統噴出 WARNING。Double cacheck 確認是新版代碼踩到驅動自己寫的 WARNING，很開心在複現 1-Day 時挖到了全新的 0-Day 漏洞，就是接下來要講的這個這個漏洞

那這漏洞感覺是在當時是因為前景本還沒有新的功能，所以應該是新盤就是在最新版的時候或是可能進兩個版本的時候加進的功能。那它的它這個用成非常簡單，是我的 ct, 因為我的 bsq 它會由一個大結構是就是那個核心的結構 cest。然後每天方式是它去維護一個 link list

那 link list, 所以我的 map 應該預期的是 3000 的 size, 就像我們前面看看它的 R 的時候呢，去想辦法去附出來的這個這個 site。那如果你點一個壞掉壞掉的不樣呢，就是不是離三的 size 的話，它 fail 掉。

然後 map handler 呢，就直接去更新這個 Q 的 reference, 然後因為它目前的 reference 是 1, 然後被更新成之後，它就被 fre 掉。那這時候前後的 c 啊或者是後面的 Q 呢，它還有一個 link 去上這個 Q object, 所以這時候就有一個在 s 成 QOB 底下的 UAF, 然後這個 UAF 的的發生的地方是在 camera 除以 2

然後實在實際在想辦法利用的時候，當然直觀就是想辦法嘗試 Heap Reclaim 或 Type Confusion 利用。但 Queue 內部成員 kctx 在操作時會被連續 Dereference 兩次，在 64 位元核心下極易崩潰，直接利用相對困難，那就在試一段時間之後就開始想一下有沒有什麼其他不好利用的方式

那這下會就試了一下試了一下之後發現有一個更簡單的方法，然後在探索這個利用方式的時候找到一個還蠻還蠻有趣的一個一些利用技巧，那接下來就是講關於這個漏洞怎麼在怎麼從一個簡單的應該說比較難應用的 UAF 呢轉換成另外一個 p, 然後再由這個做後續的利用的探索。那因為難用的 primitive 把它整好用的 primitive, 所以接下來就是有它的這個 ct 跟 Q 呢他們之間的提供的功能去做一個發向

那最後找到的方式就是 cest 呢，它就是我們前面有講到 cest 它在位，因為 Q 在 UAF 之後會有 X, 有個 P 指向指到這個 Q。那我們這時候建了另外一個 ct, 然後重在這個 queue, 然後之後呢，我們去做這種的 mememory mapping

那這種 mememory mapping 成功之後呢，我們會有一個 maping object, 那這個 maping object 也是一個結構，然後它有一個 pfect data 的 pter, 只像這個 queue, 然後同時 X 有 p 指向，所以 reference C 會做更新。那再來是 Q 它有一個還有它有 KX 的這個這個這個，那它有些操作會從這些 fdreference file, 然後就指向上面的 file

然後我們之後再把再由右邊的 c 呢把去示範這個 queue, 那因為日分是 refcount 2, 所以不少可以一，那還不會還不會換表。那我們因為有 UAF 的問題，所以我們左邊 cest 也可以去 free 掉這個 Q。那這時候這個 Q 就被 freed。那我們透過這一連串的操作呢，把我們的把我們的漏洞從一個 context 的 primitive 轉換成是 mapping 的 primitive

然後後面呢，我們再建一個 cest, 然後我們再重生那個。那這時候因為因為它因為它的因為舊的 mob 呢，它還是指就是那個那個 Q object, 那這時候因為 Q object 已經被重新建立了，所以它往上去做找這個 file object 的時候會找到的是左邊這個綠色的新的新的 file object

那因為但是這時候 F object 人只有 F 來做 reference, 所以如果這時候去 unmap 叫這個 m object 的話，那這個多層的 dereference 最後會找到新建的 object, 然後把它給釋放掉。但這時候我 FB 還有一個還還有辦法去存取它

所以我就從這個這個的 UAF 呢， MG 這個 pent 成一個 file UAF。那買過成 file UAF 之後呢，接下來做的方式就比較習慣，我用我先把 by object, 然後釋放回 istem, 然後再由其他的 object 來做 recra。那會有一些會有一些想被重的一些 object, 那這邊選擇是 p 這個 object, 因為你 tpip 在做建立的時候我講錯了，是 p 那 page 在做建立的時候，它如果把成功把這個 F 給重的話呢，那我們就可以在 uspace 透過讀寫這個這兩個 syscall 去控制這個 file object

好，那在我們已經我們假設了這個 file 呢，它對我們用剛剛前面的這個 page 的手法把它給重成pipe之後，我們接下來要從這個的這個去找到其他的可以讓我們可以成功的方式嘛。那首先我們目前還沒有任何 adress, 所以我們先想辦法去 leak address

那的話是用 bind 的這個 syscall。那 signalfd 它做他們做 bind 的時候呢，它會去檢查說我的這個它的 file 是不是一個 st。那它檢查方式是看它的 file 的 operation, 是不是只像存在於 text 的 s file operation

的這個 table。所以我就可以將 bind, 然後 f 到它回傳到 file data, 那代表說這個 file, 因為因為整個 file 都是我們可以控，那我們當我們碰到對應的值之後，然後它會傳 address, 就代表說我們猜對的這個 address, 那我們就有這個 file_operations 這個 adress

然後後面再用這個 copyuser 的這個它後面的操作去讓它產生另外 ar。那也就是我在呼叫之後，我就可以去我就可以用這個 S 的方式去 dad。那這是 AP, 那這邊一定是老透過這個 signalfd 呢去 M, 那它的它就會被寫進去。不過這邊有個限制是它的值會掉一個 c vary

那再來是 read 的話， read 的話比較相比較麻煩一點，那就會其實就是就是因為我現在有內寫跟那我就用從的 file 的這個 maping option 是我可讓它構造成是一個一列串的 ence, 那最後讓 derdiance 出來的值呢寫去 f object, 然後我就可以從用 pin 把它讀回來，所以 f, 然後讓它向我們的 object, 然後成之後呢，它的就被我們出來，那我們這時候就有一個那 code 呢，應該說我們有當然會想要知道有沒有 function code, 所以這時候就看了一下有沒有方法可以去穿這個這個類的呼叫。那這個還蠻看起來還蠻簡單的，就是 file, 然後 sops, 然後就會，然後支成一個 file table, 那就會成功了嘛

然後當它試完之後發現怎麼出現這個 kernel panic 手機整個黑掉。然後看一下這個 arr message 之後呢，發現是叫個 handle 發出來的。那 B 了半天發現好像沒有什麼頭去，所以後面就鎖性的去直接看 source 來看到底是發生什麼事情

然就發現說因為有 CFI 的關係，所以在 Android 上面的說在 Pixel 上面的 CFI, 它的作是你在呼叫一個 function 之前，它會有一個 signature 的，那如果不對的話，它就會踩到前面的一連串的，然後就會一個就會一個 exception, 然後 kernel panic。那這個東西是怎麼實作的話，怎麼實作的呢

首先每個 function 都有自己的這個 function 的 ret type 啊跟 arcument 的一些資訊，那在 C 的時候它會透過這些資訊呢算出一個一個 signatureure, 然後塞到你的 function 的開頭的前面四個 b。那像不同的 signature 就會有不同，應該是不同的 function 的行別或是呢就會不同的 sign

然後你在做呼叫的時候呢，它就會去做這個檢查，所以 match 的話就會執行，但 s 如果失敗的話不 ch 的話就會直接 kernel panic。好，那 canic 的方式是它會到這個 instruction, 然 ion 會做兩的 lup, 一個是 lookup 它的 discriminator, 然後發現是 break, 然後再來是看它的 immediate, 發現是差

然後就會去執行到 CFI header, 那 CFI header 執行到代表說出現了嘛，所以直接 kernel panic 給你看。所以觀就是我們直接改掉這個，我們直接 lookup 掉這個 instruction 就好。然後就發現說誒好像沒辦法，因為這兩邊沒有 instruction, 所以如果你 N 不掉的話，那它這邊的確就會過，然後就會進到一個無窮迴圈

那沒辦法 l, 那我就改了一個相同 s, 但是呃，但是做 map operation 的 handler。OK, 那我們的 CFI 就過了。那發現怎麼還是進到無窮迴圈，然後這時候就很納悶到底是哪出的問題。然後後面回頭看發現對到這個 S 來看，然後就看到說因為 breakbreak 這個 instruction 呢，它是 synchronous 的 exception

所以讓預期你的 handler 需要去主動去幫你更新你的 PC, 不然你會處理完。然後它回去它的 PCS 指向同個位置，你就代表說我的這個 handler 會一直被執行到。那最後的解決方式是把直接把這個這個 CFI header 改成是相同，但是是做 B 處理的這個 warning header

那這個東西它有我們要的一個操作是它在這個 function 結尾的時候會去更新我們的 PC。那這個是怎麼實作的呢？首先我們假設有兩個 instruction, 一個有兩 code, 一個是 warning, 一個是。那這兩個會拆成是第一個，這兩個拆成是兩個 instruction, 一個是 assembly code, 一個是 b, 一個是 bdl, 然後 f

那 b 的話就是就像我們前面講，它最後會接到我們的 b, 然後去 lookup 那個 table。然後記得因為它的做它做法是它看一下你的你你在 c 的時候會記錄這些有的的。記位，然後填個 table 裡面。那如果 table 裡面有對的資訊的話，它就會把這個 warning 一出來，然後更新我的 PC, 然後不做的事情

所以這時候我們就回去，然後執行執行我們對的 function。好，那如果我們能做到出類似的程式的執行流行的話，我們就可以去 p 叫這個 CMI。所以我這邊的做法是我們用前面的類寫的 primitive, 然後去 intest 一個 adress, 然後我的 F 填到代表說我在執行這個的時候呢，他發現說我的 L 呢，它的這邊寫是一個 warning, 所以我不用做任何處理

然後我在 ex 回去之後呢，我就我就能日常去執行個 function, 那這到這邊我們就有一個類的呼。那但是到這邊我就想因為我們前面是從 File 的 UAF 做發一下我們的變速 cper。那我們我們雖然變速了很多 primitive。雖然他們之間彼此有些 dependency, 但假設這些東西都 dependency 的情況下，我們真的需要多 primitive 嗎

還是其實有更簡單的方式或是只要一些只要一些 primitive, 我們就可以拿到完整的。所以我們最後在這個 page table 上面找到我們要的答案。那在講這個整個整個玩法之前呢，可能要先介紹一下這個 64 上面的 mess 怎麼做處理。那就是呢，它的體位置分的是兩區段，一個是 H, 一個是 low, 那它連會去 S 到不同的 register, 然後這些 register 會有不同的 page table, 然後它就會最後去 w 這個 page table, 然後去存取我們的 virtual address, 然後從 r 裡面的資料

那在這在 p 上面它是三層的三層的 page table。然後它用，因為我們是嘛，所以用 TTBR1。然後這個有趣另外一個有確點是這個意思。第一層的第一層的 table 呢，它叫它一個它在 kernel 內的一個 C 的變數叫做 swapper_pg_dir, 它其實是存在，然後後面的這個第二層跟第三層呢，它其實是從 c 那邊去做分配

那想到 C 呢，你就會發現可能在玩 Android 的對，或是可能那時候看關注一些花文章的人可能就會注意到說在那陣子有一個關於 6 的算是一個漏洞，算是漏洞嘛，那一個利用技巧的分享。那簡單來講它就是研究員發現說你在 6 上面的一些一些 table 的配置下，你的你其實是沒有沒有啟用任何的 SR 的，那這邊講的 SR 是在對於我們的 C 上面

所以你可以想像，你可以想像到是在這個，因為 P 剛好就符合這樣的配置，所以 P 上面的 cache 呢，它其實落在一個可預期的祭體位置。那有這個想法之後我們往回看那個 layer, layerlayer 2 跟 layer one 的 page table, 我們就會好奇說這兩個其實是不是也分配在，因為他們他們他們也是從本上面做分配

那如果在 boot time 的時候，你的狀態 noise 是比較，你的 hit 的 noise 比較少的情況下，你是不是有可能這兩個 page table, 這這兩 table 他們其實都是落在風格基體位置上。那我們就分析了一下 table 的 allocator, 然後它發現它是從 reserved memory 來做分配

然後在在經過多的測試跟研究之後，我們發現有 95% 的機率。這些 table 都會落在同一個基體位置上。好，所以我們做法很簡單，就是我們就我們我們先找到這個 page table 的記體位，然後我們有任意寫，我們就把這個寫的，我們就把 page table 的 permission 給蓋掉

那蓋成是我們從 kernel space 呢可以做 data 跟 text 的資料的協路。然後之後我們再用 write 的 primitive, 然後去我們的修。所以的 point 呢，像是這樣子，就是我們有一些我們先找到一些 page table 的進理位置，然後還有對應的修，然後我們去先改掉這個 page table 的 access permission 的 bit, 然後最後就我們要提跟我們 P 要的修

所以我們其實只需要一個任意寫就可以去在 Android Pixel 8 上面達到我們的提權。那最後 dis 流程有點長，我們這邊時間外關係，我們先跳過看一下這個 demo 的影片。那這個 do 呢，左邊是我們的手機的 mir, 然後右邊是我們的攻擊者的機器，然後這邊聽的是 ting 個 p, 然後 refer

然後我們因為前面講到這個情是一個 micious 的 APP, 然後它就可以跑錯，所以我們然後我們這邊先檢查我們的在當時的這個手機的版本。那對 malicious app 在這個 demo 下是一個貓咪相簿 App, 那就是你可以打開這個 app, 然後看一些貓咪貓咪的照片

所以你就對就使用者可能就發現，哇這個貓咪很可愛，然後就把它這個 FB 給裝起來。好像沒。然後但是就是他在看這些貓咪嘛，所以他在滑這個貓咪的 view 的過程中，他可能吞到自己。的手機已經被一個那個 reverse shell。就是我們下個 ID 看一下，然後看一下本

然後最後看一下我們的，因為我們我們 ls /data, 因為這個只有在繞過的情況下才能做才能做存取，所以到目前為止我們就完整的用這個漏洞去打到。的超那個 attack vector 是就 GPU 都被研究，那到目前為止還算是一個可以去看的東西。那後面是 Android 跟 iOS 的保護機是差蠻多的，那在 Android 上面的 memory Corruption 呢好像還可以有些利用，對

然後最後是 table 有很多很多玩法，就不管是在 Windows, 在什麼在 Linux, 在 MOS 好像都是玩自己考，所以如果找到 sandboxing 的話，不妨去試試看那邊有什麼可以玩。好，這是我的演講，謝謝大家。然後感謝。

